genblaze>=0.4.5
genblaze-core>=0.3.8
genblaze-s3>=0.3.0
boto3>=1.35.0
numpy>=1.26.0
# Scene detection
scenedetect>=0.6.5
# Vision
opencv-python-headless>=4.10.0
mediapipe>=0.10.0
pillow>=11.0.0
# Embeddings
sentence-transformers>=3.1.0
# Vector store
lancedb>=0.12.0
pyarrow>=18.0.0
s3fs>=2024.10.0
# NLP
# NOTE: pinned below 3.10 — nltk 3.10.x added an "inisec" import guard that
# misfires when the venv sits inside the project dir (it treats the venv's
# site-packages as "current working directory" and blocks deps like regex).
# NLTK_DISABLE_IMPORT_SECURITY=1 is also set at spawn time as a kill switch.
nltk>=3.9.0,<3.10
# FFmpeg
ffmpeg-python>=0.2.0
# HTTP
requests>=2.32.0
# Google Gemini AI (for high-quality image generation)
google-genai>=1.0.0
